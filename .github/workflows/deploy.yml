name: Deploy
on:
  push:
    branches:
      - master
    paths-ignore:
      - ".forestry/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: '0'
      - name: Publish to Docker Registry
        uses: docker/build-push-action@v1
        with:
          repository: ${{ github.repository }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          tag_with_ref: true
          tag_with_sha: true
  deploy:
    needs: build
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - uses: azure/setup-helm@v1
      - uses: actions/checkout@master
        with:
          fetch-depth: '0'
      - name: Upgrade helm
        run: |
          mkdir ~/.kube
          helm repo add maxisme https://${{ secrets.GH_PA_TOKEN }}@raw.githubusercontent.com/maxisme/infra/master/k8s/charts/
          helm repo update
          echo "${{ secrets.B64_KUBE_CONFIG }}" | base64 --decode > ~/.kube/config
          sha="${{ github.sha }}"
          repo="${{ github.repository }}"
          helm upgrade "${repo//./}" maxisme/web -f helm-values.yml --set="image.tag=sha-${sha:0:7}" --atomic --timeout 120s
          rm ~/.kube/config
